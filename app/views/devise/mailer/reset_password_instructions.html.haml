-# = User.find_by(email: @resource.email).name
#{@resource.email} 様
%br/
%br/
-# = @resource.email
パスワードの再設定を行います。以下のURLより設定を行なってください。
%br/
= link_to 'パスワード再設定', edit_password_url(@resource, reset_password_token: @token)
%br/
%br/
-# = link_to 'パスワード再設定', edit_password_url(@resource, reset_password_token: @token)
このメールに心当たりが無い場合や、パスワード変更するのをやめる場合は何もする必要はございません。
上記URLで設定を完了するまで、パスワードは変更されません。
%br/
%br/
サイト運営 yoyuuhi