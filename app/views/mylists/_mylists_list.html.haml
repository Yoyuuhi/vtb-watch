.list
  .list--home
    = link_to user_mylist_path do
      = icon('fas', 'chevron-circle-left', class: 'menu_icon_left')
      ホーム
  .list--vtubers
  - @mylist.vtubers.each do |vtuber|
    = link_to user_mylist_vtuber_path(mylist_id: @mylist.id) do
      .list--vtubers__name
        = vtuber.name